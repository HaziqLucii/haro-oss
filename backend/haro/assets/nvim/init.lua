-- haro bundled nvim (the LazyVim fallback for the "code" step).
--
-- This is the stock LazyVim starter, seeded into ~/.config/haro-nvim on first
-- use under an isolated NVIM_APPNAME so it never touches the user's own
-- ~/.config/nvim. Edit freely — haro only copies it in when it's missing.
--
-- bootstrap lazy.nvim, LazyVim and your plugins
require("config.lazy")
